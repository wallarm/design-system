# AI Usage Docs — Coverage Tracker

> Who's writing which `<Component>.llm.md`, and what's done. Tiering comes from the [strategy §8](./ai-ready-ds-strategy.md#8-which-components-and-in-what-order-tiering); the how-to is in the [authoring guide](./ai-ready-ds-authoring-guide.md).
>
> **Last updated:** 2026-07-02

## How to use this
1. Pick a **Deep**-tier row with no **Assignee** and put your name in it.
2. Start a fresh chat, run `/describe-component <Name>`, and work through the skill.
3. When the file is saved, set **Status** to ✅ and drop the **Doc** link in.
4. Spotted a rule that generalizes beyond the component (density, color, interaction ownership…)? Park it in the [design-judgment backlog](./ai-ready-ds-judgment-backlog.md) — it seeds the foundations layer.

**Status:** ✅ Done · 🟡 Partial / in progress · ☐ To do · ➖ N/A (not a documentable component)

## Snapshot
- **Deep tier:** 17 done, 0 partial, 1 to do (+ 1 folded). ← **The app-frame nav skeleton is COMPLETE** — AppShell (frame, once at root) + NavRail (1st-level product rail) + RemoteShell (2nd-level per-product shell + the config-driven inner-nav formation guide: `link`/`section-header`/`divider`/`group`/`drill`, the two separators, ~2–3-level depth — Artem-validated) + NavPanel (the rendered sidebar; thin doc → defers to RemoteShell) + **TopHeader (global top bar)**. Resolved Artem's component-vs-pattern question (app-shell / micro-frontend-host, verified vs the `core-frontend` host); seeded 'application-frame / host-shell discipline' + 'config-driven structural components' judgment rows. **Only SimpleCharts remains** in the Deep tier — standalone data-viz (BarList/PieChart/LineChart), not shell-family, has its own CLAUDE.md.
- **Light tier:** 41 done (Badge + Tag + NumericBadge chip-trio + Toast + Alert + Dialog + Button + ToggleButton + DropdownMenu + Link + Select + Field + Input + Textarea + NumberInput + InputOTP + InputGroup + Radio + Checkbox + Switch + SegmentedControl + **DateInput + TimeInput + DateRangeInput + Calendar** + **Pagination** + **Tooltip** + **OverflowTooltip** + **Popover** + **Drawer** + **EmptyState** + **Progress** + **Breadcrumbs** + **Tabs** + **SegmentedTabs** + **CodeSnippet** + **Code** + **Slider**), 20 parked pending · **CodeSnippet + Code = Anastasiya's first files** (the code-display family — block / inline chip / text style; seeded + completed the 7th, code-display, ladder) · **+ Loader + Skeleton (loading family) + Overlay (backdrop primitive) + Separator (divider primitive) + Text (typography primitive) tier-overridden Skip→Done** the "short note vs. skip" decision ([§13.4](./ai-ready-ds-strategy.md#13-decisions-to-confirm-together)). All got the full router treatment, not Light notes. **Actions family complete** (Button / SplitButton / ToggleButton / Link + DropdownMenu); **forms / inputs family complete** — Select + Field + Input + Textarea + **NumberInput** + **InputOTP** + **InputGroup** done (NumberInput + InputOTP confirmed the field-context gap: they don't consume `Field` context — NumberInput a11y fix task spawned; InputOTP also carries an `InputOTP`-dir-vs-`OTPInput`-export naming inconsistency; InputGroup = the single-input adornment wrapper, not a multi-field form — all flagged to Artem), **pickers complete** (Radio + Checkbox + Switch + SegmentedControl); **DATE/TIME FAMILY COMPLETE — `DateInput` (anchor) + `TimeInput` + `DateRangeInput` (type-in-place, no popover) + `Calendar` (the popover grid picker)**, all reconciled with `FormatDateTime` (display) + the [Confluence Date & timestamp display guide](https://wallarm.atlassian.net/wiki/spaces/PRO/pages/6209339432/Date+timestamp+display+guide). Family rules: value is a React Aria typed object — `DateValue` / `TimeValue` / `RangeValue` / Calendar's `DateValue[]` array (4 shapes, never a JS `Date`); `order`/`hourCycle` app-wide via `DateFormatProvider`; **field-context gap now spans 7 inputs** (NumberInput/InputOTP + the 3 date/time inputs read no `Field` context → strong foundations signal); DateRangeInput + Calendar are `alpha`. **FilterInput off-ramp DONE (pattern).** **Pagination done (Ark compound, the standalone+in-Table pager); next: completing the partial `Table` sheet (Deep, beta) — the TanStack-v8 data grid, the headline data-display component, with Pagination as its composed footer.** **(main's v0.66 merged in — EmptyState / Pagination / WallyIcon now tracked; the new `BannerIcon` sub-component + the `BulkBarSummary*` primitives fold into Banner / Table / Selection, not their own files.)**
- **Skip:** 21 (providers, layout/leaf primitives, internal plumbing).
- **New from the v0.69–0.70 main-sync (2026-07-02):** **Slider** ✅ done (merged with its own guide; verified code-accurate + 3-judge blind stress-test). **Tree** ☐ new compound — needs a guide (row below; tier TBD). **HorizontalBar** (new chart, v0.70.0 #198) folds into the existing **SimpleCharts** row — it's a sub-dir, not its own top-level component, so it can't hold its own `.llm.md`.

## Tracker

| Component | Tier | Status | Assignee | Doc | Notes |
|---|---|---|---|---|---|
| HttpMethod | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/HttpMethod/HttpMethod.llm.md) | Reference example · revised 2026-06-12 with family rules (casing trap, text-only rendition, click parents) |
| Selection | Deep | ✅ Done | — | [.llm.md](../packages/design-system/src/components/Selection/Selection.llm.md) | Covers the `SelectionBulkBar` pattern |
| Table | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Table/Table.llm.md) | **Completed from the action-bar-only partial → full router.** The TanStack-v8 **data grid** (config-driven `data` + `columns` via `createTableColumnHelper` + composed slot children). **Boundaries:** one object's detail → `Attribute`; visual/few-attribute collection → a grid of `Card`s (table↔card view toggle — same `Pagination` serves either); short non-tabular → plain list. **Cells render the dedicated display component** (HttpMethod/ResponseCode/Ip/Country/Link `type='table'`/FormatDateTime/ParameterPath in OverflowTooltip — Table is where the display family lands). **Scale: infinite scroll is the house DEFAULT** (`onEndReached`/`onStartReached` + `virtualized`); **Pagination is the deliberate exception** (composed below, Table doesn't render it). Selection via `onRowSelectionChange` → `TableActionBar` (shared BulkBar/`Selection` pattern; never red destructive, ≤1 brand+icon, one line); per-row actions → `meta.renderMenuAction` / `.display()` column, NOT the bar. **Empty:** `TableEmptyState` distinguishing no-data-yet vs no-results; **filtering lives ABOVE the grid** (`FilterInput`/search Input), not in it. **Locked:** density (no compact/comfortable prop — dense knob is Pagination `size='small'`), select/expand cols 33px, semantic sort labels per `meta.sortType`. Master-cell click → `Drawer` *preview* vs title `Link` → full page (overlay ladder). `beta` tag ignored (Artem: effectively stable). No Figma Code Connect. Blind 3-judge PANEL 9/10 HIGH → fixed the unanimous empty/filtering gap + per-row-action/card/drill-in nits. **Then a deep re-verification (Artem flagged its importance): every claim checked directly against source + a 2nd hard 2-judge panel on the advanced surface → hardened with server-vs-client sort (`manualSorting`, mirrors pagination), sticky header, virtualization≠fetch split, row-interaction precedence (select/master-cell-Drawer/title-Link coexist), pin=freeze-column / grouping=collapsible.** Code-aware clean |
| BulkBar | Deep | ➖ Folded | — | — | Internal plumbing — guidance lives in **Selection** + **Table** |
| AppShell | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/AppShell/AppShell.llm.md) | **App-frame family ANCHOR.** Real DS component that *embodies the app-shell / micro-frontend-host pattern* (Artem's component-vs-pattern Q, resolved against the `core-frontend` host): persistent global chrome (grid — header top / rail left / remote content) **mounted exactly once at the app root** (verified — `<AppShell>` appears once in all of core-frontend, `apps/host/.../Shell.tsx`, whose own comment reads "host owns only cross-product surface"). **#1 boundary:** a single product screen → `RemoteShell` + content, **never its own AppShell** (the remote is blind to the shell it mounts into). Page insides → Stack/Flex; in-product nav → RemoteShell/NavPanel; **AppShell has NO feature props** — search/tenant/notifications/help compose into `TopHeader`, account/settings into `NavRail` footer. **Entrance `reveal`/`expandFrom` = first-app-load ONLY** (splash→morph; LoginFlow/RevealFlow stories), off by default (core-frontend doesn't even wire it yet). Prototyping: render content straight into `AppShellRemote`; MF/Module-Federation is production wiring. Hardened the `AppShellRemote`(slot)↔`RemoteShell`(product frame) naming seam. Figma 9257-100 (15k×15k spec — metadata times out; read macro + Notes scope: login/splash, global-search [later], tenant [later], dataplane [standalone MFE], 2nd-level-nav/shortcuts/dark-UI [shipped]). Consumes DS 0.66.2. Blind 3-judge panel 7/8 HIGH → fixed the unanimous search/tenant-seam MED + slot-naming gloss; code-aware caught an account-in-rail-footer drift. Seeded the **app-frame** judgment-backlog row. Pairs: TopHeader/NavRail/RemoteShell/SplashScreen |
| Attribute | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Attribute/Attribute.llm.md) | Object-metadata grid; read-only host for value-slot display components; `AttributeActions` for the click; flags Figma inline-edit + error state as not-yet-shipped |
| Banner | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Banner/Banner.llm.md) | Page-top full-width system/account-wide notice; "still relevant on another page?" test splits Banner vs Alert; primary dark / secondary light, no success; one action pattern; institutional voice. Closes the messaging family (Toast/Alert/Banner; Dialog pending) |
| Country | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Country/Country.llm.md) | First compound in the family; sanctions flag-only / name-only forms |
| FilterInput | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/FilterInput/FilterInput.llm.md) | **Documented as a PATTERN, not a component** (Artem's framing; Storybook files it under `Patterns/FilterInput/`). Config-driven **query builder** — field→operator→value autocomplete builds an **`ExprNode` tree** (`Condition`/`Group`, AND/OR + grouping) rendered as removable chips; `onChange` emits the tree (not a string) for a backend query API. **Lead boundary (Artem's emphasis + agnostic research):** simple filtering (≈3–5 facets, pick-from-list, live results) → a row of `Select`/`SegmentedControl`, NOT this — **no dedicated lightweight "filter bar" component yet** (likely future addition; the user's planned "simple version via select menus"). Use-it-at-all = two axes (attribute count OR boolean/operator complexity). **It's a pattern = needs infra:** `fields: FieldMetadata[]` (ideally a backend query-metadata endpoint; hardcode OK for prototypes), field `type` drives operators, `values`/`options`/`getSuggestions` drive value autocomplete. **Locked:** operator labels house-fixed (`=`→"is", `in`→"is any of", `is_null`→"is set"…); chips/connectors/edit/delete automatic; does NOT read `Field` context (it's a filtering surface, not a labelled field); **`status_code` reserved field name** auto-wires HTTP-status helpers + DS overrides consumer callbacks (opt out = different name + `createStatusCode*` factories); query is portable as text (select-all copy → paste re-parses — target of ParameterPath's copy). **Judgment:** `strictValues` (allowlist vs suggestions; Attacks page = false), `externalErrors`+`onErrorsChange` (backend-rejected values → red chips, presentational, pair w/ `Alert`), `placeholder`/`showKeyboardHint`. NOT tagged alpha (production, actively maintained — June `strictValues`/`externalErrors` additions). Real export `FilterInput` (March ref doc's `filter/FilterField` was stale). Internet research consulted (query-builder vs live-filtering; ~3–5 dropdown sweet spot). Figma node 5824-78418 (huge spec page). Blind 9/10 HIGH (1 MED = the not-yet-built simple-filter-bar off-ramp), code-aware clean |
| Ip | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Ip/Ip.llm.md) | Richest compound yet: slot order, IpList overflow, neutral source chips, standalone IpProvider |
| NavPanel | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/NavPanel/NavPanel.llm.md) | App-frame sibling — the **inner sidebar** (`<aside>`, resizable ~216px) `RemoteShell` renders from a `NavConfig`. **Thin rendering-layer doc: defers the 'how to form inner nav' decision to RemoteShell (no duplication).** Lead: **you don't build it by hand — author the RemoteShell config; the parts are an escape hatch** for a custom sidebar the config can't express. Parts: `NavPanelHeader` (product label) · `NavPanelItem` (link, icon optional, auto active/inactive) · `NavPanelSectionHeader` (header separator) · `NavPanelDivider` (line) · `NavPanelGroup`(+Label/Content/Item, collapsible+connector) · `NavPanelBack` (drill-up) · `NavPanelSkeleton`. Boundaries: global rail → NavRail; standard inner nav → RemoteShell config; not a standalone sidebar. Single-judge check 6/6 HIGH (clean; trimmed the half-specified resize range). Pairs: RemoteShell/NavRail |
| NavRail | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/NavRail/NavRail.llm.md) | App-frame sibling (fanned out from AppShell). The **global product rail** (`<nav aria-label='Global navigation'>`) in `AppShellRail` — first-level product switcher. **Boundary:** global product switch only — in-product section nav → `NavPanel` (inside `RemoteShell`); in-page panel switch → `Tabs`/`SegmentedControl`; items *navigate* (`<a href>`), not toggle. **Locked:** `NavRailBody` (scrolls) + `NavRailFooter` (pins; settings/theme/help = plain `NavRailItem`s, Recent/Account *menus* = app-composed `DropdownMenu`s, not shipped parts); `NavRailItem` = link w/ required `icon` + auto right-`Tooltip` (label+shortcut, essential when collapsed) + `active`→`aria-current`; **collapse = width toggle 184↔48 icon-only**; roving arrow-keys + **ArrowRight hands focus into `NavPanel`** (rail↔panel seam). **Judgment:** `collapsed` adaptive (expanded on Home, collapsed inside a product — core-frontend `ShellNav` confirms); per-item `shortcut` *sequence* (`['G','H']` jumps straight; distinct from arrow roving). Prototyping: hardcode items (real list from runtime-config `getProductEntries`). No Code Connect/node in code (Figma 'Component→Rails' frame on the 9257-100 spec page, unread). Single-judge check 6/8 HIGH → fixed footer-utility + keyboard-contrast MEDs + a garbled line. Pairs: AppShell/NavPanel/DropdownMenu/Tooltip+Kbd |
| ParameterPath | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/ParameterPath/ParameterPath.llm.md) | Not-for-URL-routes boundary; built-in copy→filter + truncation locks |
| RemoteShell | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/RemoteShell/RemoteShell.llm.md) | App-frame sibling — the **per-product content shell** (2nd-level nav), home of the **inner-nav formation guide** Artem requested. **Config-driven:** author a `NavConfig` (`productLabel`+`items`+`headerActions?`), pass as `config`; RemoteShell builds panel+breadcrumb+content+active+drill/back (+ `basePath`/`onNavigate`). **Item vocabulary (the guide):** `link` (route) · `section-header` (named group heading = header-as-separator, **prefer for grouping**) · `dividerAfter:true` (unlabelled **line**, sparingly — utility tail) · `group` (collapsible, indented+connector) · `drill` (**enter an entity scope** → children + auto '← Back' + breadcrumb scope-switcher via `entities`+`param`). **Recommendations (Artem-confirmed):** flat product = links only; section-headers first; dividers only when nameless; drill only for entity scopes; **~2–3 levels of panel depth** (breadcrumb+Back carry depth). **Boundaries:** global product switch → NavRail; outer frame → AppShell (remote NEVER renders its own AppShell); page insides → Stack/Flex in `RemoteShellContent`. **Figma-ahead/WIP (designer notes):** content page-header ('common header, might change' → hand-build inside content, don't extract); breadcrumb scope-switcher **menu** WIP (data+label ship, clickable switch not final). Verified vs core-frontend remote-template + host Settings. No alpha/beta; no Code Connect (Figma 11294-4725 examples + 9303-1857 item matrix). 2-judge blind panel (10/10 + 7/10 HIGH) → fixed divider self-contradiction + flat-floor/page-header-interim/WIP-behavior/drill-entity/panel-depth nits; code-aware verified the full NavConfig firsthand. Pairs: AppShell/NavPanel/NavRail/Breadcrumbs |
| ResponseCode | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/ResponseCode/ResponseCode.llm.md) | Sibling of HttpMethod; adds mask + text-only-rendition guidance |
| SimpleCharts | Deep | ☐ To do | — | — | Standalone data-viz family (BarList / PieChart / LineChart + **HorizontalBar**, added v0.70.0 #198). Has its own CLAUDE.md. HorizontalBar folds into this one file (it's a `SimpleCharts/` sub-dir, not its own top-level component). |
| SplitButton | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/SplitButton/SplitButton.llm.md) | Actions-family sibling of Button; thin grouping **wrapper with no styling props** — variant/color/size live on the two child Buttons, both on one matrix cell; fused look automatic; chevron = `DropdownMenuTrigger asChild`, menu **composed not owned**; left = common action + menu of *variations*. Figma node 8485:8977 (Notes frame empty). Seeded judgment-backlog "grouping-wrapper discipline" row |
| TopHeader | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/TopHeader/TopHeader.llm.md) | App-frame sibling (closes the shell) — the **global top bar** in `AppShellHeader`: `TopHeaderLogo` (interactive, links home, `href`/`asChild`, needs `aria-label`) left + `TopHeaderActions` (flex row, divided by `TopHeaderSeparator`) right, `justify-between`. **Right cluster = global cross-product affordances composed IN** (not TopHeader parts): search `Input`/`Kbd` ⌘K · tenant switcher · notifications `Button`+`Tooltip` · help `DropdownMenu` (per core-frontend `ShellTopBar`; global-search/tenant still landing). **Boundaries:** account/settings/theme = **`NavRail` footer** (user-scoped, not global chrome), NOT here; a page's title/tabs/actions = inside `RemoteShellContent` (per-page, Figma-ahead); product nav → NavRail/RemoteShell; not a per-view toolbar. Locked: logo-left/actions-right fixed, logo interactive. No size/variant (layout bar). No Code Connect/Figma node; no stories (via AppShell stories). Single-judge check 7/7 HIGH (clean; added the account-vs-top-bar 'why'). Pairs: AppShell/NavRail |
| Tour | Deep | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Tour/Tour.llm.md) | The guided-onboarding overlay (Ark UI Tour). **Config-driven, NOT composition-driven** — drive it via a `steps` array + `useTour` + one `<Tour tour={tour}/>`; sub-components are internal/unexported (only `<TourClose>` composable, for analytics). Two step `type`s: `dialog` (centered welcome/finish) + `tooltip` (anchored coachmark). **Lead boundary (research-backed): reach for the lightest tool first** — `Badge` "New" / `Tooltip` / `Banner` for a callout, single `beaconStepEffect` for passive discovery, a multi-step Tour ONLY for a genuinely unfamiliar / hands-on workflow; never feature-dump or blanket-fire on load. **Component owns presentation, NOT orchestration** — trigger timing, show-once / frequency-cap, step-count, and measurement are the consumer's / a skill's job (the "does NOT own" section). Effects: `beaconStepEffect` (passive, click-to-reveal) / `waitForStepEvent` (interactive "do-it" gate; `delay` is a debounce, not a timer — no time-only auto-advance). Content-heavy → carries onboarding-register microcopy (benefit-first 3–5-word titles, ≤160-char desc, value-first, never "OK", Skip always) from its **own** Figma "Content recommendation" frame (Notes frame all-resolved). No Code Connect; no alpha/beta tag (in root barrel). Internet + DS-competitor research consulted (NN/g, Primer feature-onboarding, Atlassian Spotlight, Pendo, Chameleon stats, react-joyride parity). Blind 3-judge panel → all-HIGH after fixing 5 **convergent** gaps (timed-advance/`delay`, auto progress text, beacon=click-not-hover, single-feature over-reach, `closeOnInteractOutside` default); code-aware clean. Seeded the **onboarding/feature-guidance ladder** (5th ladder) + a **component-vs-governance** judgment row + 2 onboarding-register content rows. **Companion: the `craft-onboarding` skill** (`.claude/skills/craft-onboarding/`) — v1 built, then **enhanced to v2** (self-improving + PM-autonomous: gate-first + target-discovery + Storybook preview + launch-contract governance + `FIELD_NOTES`/`HARVEST` self-learning loop) and **eval'd twice**: iter-1 tied a doc-armed baseline (100/100 — the Tour doc carries the judgment) → harder iter-2 **100% vs 91% baseline (+0.09)**, with the loop already catching a real Recipe-D bug (portaled `Select` click doesn't bubble). |
| Accordion | Light | ☐ To do | — | — | |
| Alert | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Alert/Alert.llm.md) | Inline/in-context messaging (in Drawer/Dialog/under header); messaging-ladder boundary vs Toast/Banner/Dialog/Field; locked semantic color+icon, secondary/small button recipe; actions top/bottom by width; 3 TBDs (loading/stepper/promo) |
| Badge | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Badge/Badge.llm.md) | Base of the domain-chip family → got the full router treatment, not a Light note |
| Breadcrumbs | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Breadcrumbs/Breadcrumbs.llm.md) | The hierarchical **location trail** for a deep (~3+ level) nesting, in the **page header**; in `AppShell` / `RemoteShell` it's **composed from route segments** (`RemoteShellBreadcrumb`), not hand-built per page. Compound: `Breadcrumbs` (`<nav><ol>`, **auto-inserts separators + auto-marks the last child current**) + `BreadcrumbsItem` (ancestor = `<a href>`; **last = current page, non-link `<button>` w/ `aria-current=page`, no href**) + `BreadcrumbsEllipsis` (collapse long trails, keep first + current) + `BreadcrumbsScopeSwitcher` (a `DropdownMenu` item for tenant / scope switch; can be the current level). **Boundaries:** flat / shallow (1–2 levels) → skip; peer / sibling views → `Tabs` / `SegmentedTabs` (vertical hierarchy only, never lateral); complements primary nav / back, never replaces; steps → stepper. **Figma-ahead (not shipped):** per-item truncation + tooltip (items `whitespace-nowrap` only) + the slash separator (arrow/ChevronRight is the auto default). Consulted GitLab Pajamas (vertical-only, collapse, last = current, sentence case). Code Connect (node 821-16083). Blind 8/8 HIGH, code-aware clean. |
| Button | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Button/Button.llm.md) | Workhorse anchor of the Actions family → full router file (Button vs `Link`/`ToggleButton`/`SplitButton`/`Select`'s `SelectButton`). Locked **partial color×variant matrix**; two-axis **emphasis(`variant`)×intent(`color`)** model + one-primary-per-view; Save=`primary`/`brand`, Cancel=`ghost`/`neutral`; `neutral-alt` for dark surfaces (Toast). Figma node 36:2304 |
| Calendar | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Calendar/Calendar.llm.md) | **The pop-up date/range grid picker** (Ark UI `DatePicker`); **closes the date/time family.** Compound (~18 parts), tagged `alpha`. **Trigger composed** — your own `<Button variant='outline' color='neutral'>` in `CalendarTrigger` (Calendar ships none); **`CalendarContent` IS the popover** (Portal+Positioner+animation, like DropdownMenu/Popover — never wrap in `Popover`). **Value = `DateValue[]` ARRAY** (1 single / 2 range) — a THIRD value shape vs DateInput's `DateValue` and DateRangeInput's `{start,end}` (real cross-component trap); `today(getLocalTimeZone())`, never JS `Date`. **Composition spine** = Calendar > CalendarTrigger(+Button) + CalendarContent > CalendarBody > CalendarGrids (1/2 months auto by `type`); **optional layers** = `CalendarPresets` (left sidebar, defaults auto by type, shortcuts auto), `CalendarInputHeader` (embeds DateInput(s) for type-or-click), `CalendarFooter`(`CalendarKeyboardHints`+`CalendarFooterControls`>`CalendarResetButton`+`CalendarApplyButton`) — the 10 Code Connect combos = type × {basic/+input/+presets/+presets+input/+full}. **Apply closes + Reset clears are BUILT-IN** (your onClick is additive — don't re-wire). `closeOnSelect` default true → set **false** for range/footer/Apply (+ controlled value/open). `min/maxDate`, `isDateUnavailable`, `disallowDisabledDatesInRange` (controlled), `showTime`, `readonly`+`defaultOpen` (display-only grid). Boundaries: known date → DateInput, typed span → DateRangeInput, text timestamp → FormatDateTime. Figma node 2010-5 (spec) / 2233-8455 (Code Connect); `order`/`hourCycle` from DateFormatProvider. Blind 10/10 HIGH, code-aware clean |
| Card | Light | ☐ To do | — | — | |
| Checkbox | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Checkbox/Checkbox.llm.md) | Multi-select / independent yes-no twin of Radio. **Standalone Checkbox is first-class** (consent / terms / remember-me — no group, no value); `CheckboxGroup` (optional) = short multi-select set, value is an array. **Tri-state `checked`** (`true|false|'indeterminate'`) — indeterminate is a state YOU set for select-all/parent (no shipped aggregation; the select-all sits outside the group). **Boundaries:** one-of-set → Radio; immediate on/off setting → Switch (consent stays Checkbox); long multi-select → multiselect Select (removable Tags). Self-labels → no FieldLabel; set name = `FieldSet`+`FieldLegend` (canonical; `Field`+`FieldLabel` also works via the auto-swap). **Locked:** `CheckboxIndicator` zero-prop/manual, `variant='default'|'card'` on the group (taste; card text-only), a description → 2-col grid, set is vertical-column-only (no horizontal set layout), validation on the wrapper. Emphasis = `Badge` in `CheckboxLabel`. Figma + 2 Code Connect files. Workflow-assisted (8-agent grounding + 3-judge panel → all-HIGH after 2 fixes; code-aware clean) |
| Code | Light | ✅ Done | Anastasiya | [.llm.md](../packages/design-system/src/components/Code/Code.llm.md) | The **monospace text style** (`font-mono` `<p>`/`asChild`) — the chrome-free **lightest rung** of the code-display ladder, mono sibling of `Text`. **Thin router** (per Anastasiya + designer: "Code is just a text style"). **Reach for it:** a mono value with **no affordance** — code-shaped value in a `Table` cell, ID/hash/token/path in a label/`Attribute`, version, header value (machine values mono, human words → `Text`); use it over hand-rolled `<span className='font-mono'>`. **Climb the ladder:** inline *chip* (bg, optional copy — **the bg is the cue, not copyability**) → `InlineCodeSnippet`; standalone block / any affordance → `CodeSnippet` (only a chrome-free multi-line mono paragraph stays `Code`). **Locked:** pure typography (no bg/copy/highlight/border); `size`/`weight` follow surrounding type; `color` follows meaning (`destructive` only for an error value, never decoration). Figma Foundations 2080-14637 ('Code — typography scale': code-l/m/s/xs). References skipped (typography commodity; ladder enrich done on CodeSnippet). Single blind judge 6/6 HIGH, no flags; code-aware fixed a ParameterPath over-claim (only `Ip` renders on `Code` internally — verified `IpAddress`/`IpPort`). Completes the code-display ladder |
| CodeSnippet | Light | ✅ Done | Anastasiya | [.llm.md](../packages/design-system/src/components/CodeSnippet/CodeSnippet.llm.md) | **Anastasiya's first file.** The read-only multi-line **code block** + covers **`InlineCodeSnippet`** (the inline chip), exported from the same dir so its guidance lives here (generator keys by top-level dir). **3-rung code-display ladder:** plain mono *text*, no bg/copy/highlighting → `Code` (text style); inline *chip* (bg+rounded) → `InlineCodeSnippet` (**the chip/bg is the decider, NOT copyability** — copy is a per-product-case toggle that can be off); standalone multi-line block → `CodeSnippet`. Composition spine `Root code=… language=…` > `Content` > `Code`, layer line-numbers / `Header`(`Title` filename) / tabs (**consumer owns tab state, swaps `code` AND `language`** — tabs store no bodies) / `Actions` (prebuilt Copy/Fullscreen/Wrap, float top-right or in header) / `maxLines` auto show-more (explicit `ShowMoreButton` replaces auto) / `getHttpFolds`. **Locked:** read-only display; copy+confirmation-tooltip built in; **highlighting OFF until an adapter is wired** — per product-case/language, **in code not Figma** (Prism ~15KB default / hljs ~30KB / Shiki ~200KB WASM); `--color-syntax-*` foundations/code-managed (don't re-point to a mockup); wrap=`ToggleButton` off by default; fullscreen=portal+Esc; annotation colors fixed enum (Figma "Error" = prop `danger`). size default `sm` (story label "md default" is stale). **Figma-ahead:** `code-snippet-field` (Error On/Off) not shipped → **compose** `Field`+snippet (read-only; error is external/presentational). Figma 2902-11021 (Components) + Code Connect (`CodeSnippetRoot` 3087-29516, `InlineCodeSnippet` 2983-55138). Enrich: Geist (InlineCode/Snippet/CodeBlock split) + Carbon/PatternFly. Blind 3-judge panel (7 prompts × 3 = all-HIGH) → 5 convergent/lone fixes; code-aware clean. Seeded the **code-display ladder** (7th) in the judgment backlog |
| DateInput | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/DateInput/DateInput.llm.md) | **Anchor of the date/time family.** Type-in-place segmented date field (React Aria `useDateField`) — **no calendar popover** (leading calendar *icon* is decorative); month-grid / presets → `Calendar` (which itself embeds a `DateInput` in its `CalendarInputHeader`, so they're complementary not rival). **Value is a React Aria `DateValue` (`CalendarDate` / `CalendarDateTime`), NOT a JS `Date` / ISO string** — `today(getLocalTimeZone())` for "now" (the #1 AI trap). **`order` (day/month-first) + 12h/24h are app-wide via `DateFormatProvider`, never per-input** (no such prop exposed; no provider → day-first + locale). **Field-context gap** like NumberInput/InputOTP (no `useFieldContext` → no auto-wire) + `error` paint-only double-wire; `FieldAction` = "Set now" home. **Has a real size axis (36/32/24)** — unlike Input/NumberInput. Judgment knobs: `granularity` (day→minute→second), `showTimeDropdown`+`timeStep`, `min/maxValue`, `placeholderValue` (start-segments, not default). Boundaries: time-only→TimeInput, span→DateRangeInput, display→FormatDateTime. Figma 114:2110 (Artem: Notes/content frames carry nothing beyond the Confluence display guide). Blind 8/8 HIGH, code-aware clean |
| DateRangeInput | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/DateRangeInput/DateRangeInput.llm.md) | The typed **continuous span** (start–separator–end in one bordered control; React Aria range field). **Compound** — all-in-one `<DateRangeInput>` *or* compose `DateRangeProvider` + `DateRangeStartValue`/`DateRangeSeparator`/`DateRangeEndValue` + `useDateRangeContext` for custom layouts. **Tagged `alpha`** (API may move). Value = `RangeValue<DateValue>` (`{start,end}` of CalendarDate/CalendarDateTime), null on clear. `granularity` applies to **both** ends. **Renders no error text itself** → must pair `Field`+`FieldError` (same field-context gap). **No `min/maxValue`/`placeholderValue` props yet** (unlike DateInput/TimeInput — pre-fill via `value`/`defaultValue`, bound via onChange validation). Boundaries: two *unrelated* dates → two DateInputs (not a span); grid+presets range → `Calendar type='range'` (ships DEFAULT_RANGE_PRESETS, embeds 2 DateInputs); display → 2× FormatDateTime. Blind 8/8 (3 MED→fixed: default-vs-placeholder + bounding fallback), code-aware clean |
| Dialog | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Dialog/Dialog.llm.md) | Reframed (Artem): a **layout** component, not messaging — centered modal twin of Drawer (wraps it, minus resize). Owns the overlay/layout ladder (Dialog→Drawer→page); destructive recipe; sizes 400/560/960. Export gotcha: not in root barrel, reachable via `/Dialog` subpath. **Revised 2026-06-24 (Drawer pass):** footer can carry a **left** secondary control (a "Don't ask again" `Checkbox`/`Switch` via `DialogFooterControls placement='left'`); header info-`Tooltip` is the default affordance; richer header (tabs/segmented/progress/stepper, header action buttons, prev-next paging) flagged **Figma-ahead**; sharpened the distinction that `Drawer` can run **non-modal** + resizes while Dialog is always modal/centered. |
| Drawer | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Drawer/Drawer.llm.md) | The right-anchored **side panel** for an object's detail / preview / contextual edit **while keeping page context** (Ark UI Dialog primitive; many sub-parts). **Our `Dialog` is built on this Drawer** (centered + modal, minus resize — code-verified `Dialog.tsx` renders `<Drawer>`). **Key differentiator from Dialog: can run NON-MODAL** (`modal={false}` + `overlay={false}`) so the page stays interactive beside it (a preview next to a live `Table`), and it's **resizable** (`DrawerResizeHandle`, left-edge drag, clamp 400–1130). **Sizing via `width`** (px/% — NOT a size enum; presets 640/960/1130); defaults modal+overlay true (blocks, page still visible). **Ladder:** anchored hint → `Popover`; short blocking decision → `Dialog`; **object detail/preview/edit beside the page → Drawer**; full detail → page. **Locked:** right-anchored only (no left/top/bottom), fixed look, header (title + auto `DrawerClose` + default info `Tooltip`) / scrolling body (auto scroll separators) / footer (`DrawerFooterControls`: left secondary control + right Cancel-ghost + one primary). **Tabs-in-header SHIPPED**; other 2nd-line (segmented/progress/stepper) + prev-next paging Figma-ahead. Nesting pushes back. No Code Connect; Figma 2507-16887 (Notes: sizes/overlay-on-off/X-placement/prev-next all TBD). **Consulted GitLab Pajamas** (drawer = supporting context beside page; ours defaults *modal* vs Pajamas non-modal — documented ours). Blind ladder panel (with Dialog) 8/8; code-aware fixed the lineage claim. |
| EmptyState | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/EmptyState/EmptyState.llm.md) | The **house "nothing here" state** for any empty region (Table/list/Card-grid no rows, search/filter no-match, empty Select/DropdownMenu, chart no-data, first-use zero-state). Compound (Illustration[icon-in-circle] · Message[Title+Description] · Actions · Link), `role=status`, scales full → one-line filler. **Two shipped `type`s: `collection-empty` (256–560) / `no-results` (240).** **THE decision = *why* it's empty → which type + whether there's a CTA:** first-use → create CTA; empty/completed → CTA-light (celebratory if completed); **no-results → "Clear filters", NEVER create** (#1 documented mistake); error → alert-icon + Retry (or `Alert`/error page if a hard failure); restricted → unlock path, no create. error/permission = *compose* (not a shipped type); compact in-container empties stay CTA-light. Illustration = small icon-in-circle (multi-ring is Figma-ahead). `TableEmptyState` composes it for grids. **Guidance split per research (Atlassian/Polaris model): component doc owns type→CTA; copy formula → content-guidelines "Empty-state copy" section; type decision → judgment-backlog row. NOT a separate skill** (it's a section of the future content/UX-writing skill). No Code Connect; Figma 7426-193270. **Multi-DS research** (Pajamas/Atlassian/Polaris/Carbon/Intuit/NN/g). Blind 8/8 (5 HIGH + 3 MED→fixed: in-container CTA-light, error-vs-hard-failure threshold, multi-widget dashboard, empty-vs-completed tone); code-aware clean. |
| DropdownMenu | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/DropdownMenu/DropdownMenu.llm.md) | Actions-family; **trigger-agnostic** menu of commands (button / icon / right-click `DropdownMenuContextTrigger` / `anchorPoint`); boundary vs `Select` (form-field value) — but a menu *may* nest a value-pick (radio-group / submenu, e.g. Language); custom UI → `Popover`→`Dialog`/`Drawer`. Item intent = `variant` (destructive/brand), `onSelect` not onClick; portal/position/scroll automatic; **no size system (design-TBD)**; checkbox items stay open. 18 parts (Ark UI). Figma 267:5551 (Notes = design TODOs). Extended overlay-ladder backlog row |
| Field | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Field/Field.llm.md) | Full **router** file: one doc covers the single labelled field + grouping parts. **Route:** single field (`FieldLabel` + bare `Input`/`Textarea`/`Select` + `FieldDescription`/`FieldError`) / grouped controls answering ONE question → `FieldSet`+`FieldLegend` (real fieldset/legend = group's a11y name) / multi-field layout → `FieldGroup` (+ decorative `FieldTitle`/`FieldSeparator`, no semantics). Test: do the controls answer one question? **Boundaries:** self-labelling controls (Checkbox/Radio/Switch) don't take a `FieldLabel` (double-labels); `Input`/`Textarea`/`Select` ship no label → need Field; `FieldError` = bottom rung of the messaging ladder (form-level → Alert). **Locked:** auto id/htmlFor (Input/Textarea only — **NumberInput is the exception, doesn't consume context yet**), `required` single source, `FieldIndicator` zero-prop (`*`/`(Optional)`), `FieldError` fixed danger+alert, CheckboxGroup auto-fieldset, emergent spacing. **Traps:** invalid state is double-wired (`<Input error>` + `<FieldError>`); error replaces description (manual swap). Required/optional both sanctioned, consistent per form. Char counter + success state Figma-ahead → don't build. Figma node 91:2. **3 code issues for FE lead:** invalid double-wire, `aria-describedby` not tied, NumberInput no field-context. Workflow-assisted (8-agent grounding + 3-judge panel → all-HIGH, 1 code-aware fix) |
| Input | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Input/Input.llm.md) | The single-line free-text control — a bare label-less `<input>` owning only its box (`h-36`, `w-full`, `error` is the ONLY variant, **no size axis**). **Router:** multi-line → `Textarea`; numeric + steppers → `NumberInput` (not `type=number`); pick-from-list → `Select`; fixed-length code → `InputOTP`; **adornments (icon/prefix/suffix/clear/password-toggle) → `InputGroup`, never props on Input**; no Search/Password component; no free-text tag/token input (multi-from-list → multiselect Select; filter → FilterInput). Needs a `Field` for label/description/error (auto-wires id/`required` via context — never hand-wire `id`/`htmlFor`); placeholder ≠ label. Validation = double-wire (`<Input error>` + `<FieldError>`). Figma node 94:49; Code Connect = 3 examples (default/error/disabled). **Bug spawned (`task_01ebec27`):** Input.tsx double prop-spread `{...mergedProps} {...props}` clobbers the Field-context merge (chained handlers dropped). Workflow-assisted (8-agent grounding + 3-judge panel → all-HIGH, code-aware clean) |
| InputGroup | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/InputGroup/InputGroup.llm.md) | The single-input **adornment wrapper** (shadcn-style: `InputGroup` + `InputGroupAddon` + `InputGroupText`) — owns the box (border / focus ring / error paint) and **strips the inner `Input`'s own box** (via `data-slot="input"` overrides) so the Input becomes the fill. **CATEGORY CORRECTION (Artem's mental model was "a combination of inputs / a form"):** it is NOT a multi-field/form container — that's `Field`+`FieldGroup`; InputGroup adorns ONE control. Surfaced + folded into the doc's boundaries; his "how to compose a form" foundation thought maps to the Field family, not here. **Reach for it when:** an `Input` needs an in-box affordance (leading/trailing icon, text prefix/suffix `https://`/`.com`/unit, inline clear / show-hide / submit button, ⌘K `Kbd` hint, `Loader`, embedded scope/unit `Select`, help `Tooltip`) — the ONLY sanctioned home for these (no `icon`/`prefix`/`clearable` props on Input; no dedicated Search/Password component). **Locked:** group owns the box (don't restyle the inner Input border); **`error`/`disabled` go on the inner `Input`** (group reflects via `has-[aria-invalid]`/`:disabled`) + family double-wire; addon `align` inline-start(def)/inline-end, `variant` ghost(def — icons/buttons/Kbd/tooltips)/outline(text prefixes + embedded Selects); addon-click focuses the input. **Boundaries:** label/description/error chrome → still `Field` (InputGroup nests inside it); `Textarea` takes NO adornments (per its doc) despite a CSS `has-[>textarea]` escape hatch; an embedded `Select` is a *secondary* scope/unit picker, never the main value. **FLAGGED:** InputGroup ships `size` default(h-36)/medium/small but bare `Input` has no size axis (fixed h-36) → smaller sizes unproven, stories only use default; flagged to Artem. NO Figma/Code Connect (Artem: technical/code-only component, nothing to share). References skipped (shadcn pattern; stories + Input-family contract settle it). Workflow-assisted (3-judge blind panel + code-aware → code-aware clean, 1 fix assigning the inline-Button addon variant → all-HIGH). Completes Input's adornment cross-ref |
| InputOTP | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/InputOTP/InputOTP.llm.md) | One-time-code / PIN field (Ark UI `PinInput`); the rare verification/approval-code control. **Naming trap:** dir + importPath + metadata name = `InputOTP`, but the exported symbol is **`OTPInput`** (`import { OTPInput } from '@wallarm-org/design-system/InputOTP'`) — flagged to Artem as an export-consistency issue. **Boundary (panel-driven fix):** the defining trait is **single-use** — a fixed-length *stored* identifier (credit-card / account / IBAN / ID / phone) → `Input`, NOT OTP (fixed length alone isn't the cue); free/variable text → Input, password → Input password (sensitive *code* → keep OTP + `mask`). **Locked:** built-in OTP keyboard logic (auto-advance / paste-fills-all / backspace), mono+uppercase+`h-36` cells, **one size**, em-dash separator, states "as in text input"; `error`→Ark `invalid` + double-wire. **Composition (product-case):** `count` (4–8, def 6) + grouping `groupSize` (sanctioned `XX—XX—XX` def 2 / `XXX—XXX` 3 / `XXXXXX` 0); set **`type` explicitly** (`numeric` for digit codes — code default is `alphanumeric`), **`otp`** for SMS autofill, **`mask`** for sensitive; value is `string[]`, `onValueComplete` = submit trigger. **Field-context gap (like NumberInput):** doesn't consume `Field` context → label/aria/`FieldError` don't auto-wire. `FieldAction` in the label = the "Resend code" home. Figma node 10542:65 (Documentation frame = the 3 layouts + "states as in text input" + "combinations standard as in all other inputs"); no Code Connect. Workflow-assisted (3-judge blind panel + code-aware → code-aware clean, 2 fixes for the credit-card/identifier boundary → all-HIGH) |
| Link | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Link/Link.llm.md) | Actions-family **navigation** member (closes the family). Element-by-behavior boundary (navigates → Link; acts / no-`href` → Button; link-as-button → `Button asChild`); 4 semantic `type`s incl. niche `table` (master-cell title link, navigates to object); underline on hover only (color-only-at-rest = conscious WCAG 1.4.1 trade-off, parked); icons as children (trailing = affordance, leading = identifies destination); **no `newTab` prop** (native `target`+`rel`+icon+a11y name); size + weight match surrounding text. Workflow-assisted (enrich + draft + 5-lens verify). Figma 923:5271 |
| NumberInput | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/NumberInput/NumberInput.llm.md) | The numeric sibling of `Input` — the Input box restricted to a number format + an always-on increment/decrement stepper (Ark UI NumberInput; `<div>` root wrapping a real `<input>` + two stepper buttons). **Boundaries:** numeric-that's-really-an-identifier (phone/port/ZIP/version/card — leading zeros / no arithmetic) → `Input`; small fixed number set (page size 10/25/50) → `Select`/`SegmentedControl`/`Radio`; approximate/felt value or min–max range → **`Slider`** (exact figure stays NumberInput; feel *and* figure → Slider + `SliderInput`). **Locked:** stepper always rendered (no hide prop — if +/– is meaningless, that's the signal to use Input); box matches Input but **no size axis**; `error`→Ark `invalid` (paint only, no words); **`value`/`defaultValue` are STRINGS** and uncontrolled renders **`0`** not empty (`defaultValue` defaults to `'0'` — pass `defaultValue=""` for blank). **`min`/`max`/`step` + `formatOptions`** (Intl currency/percent) are the judgment knobs. **Field-context gap — the headline difference:** unlike Input/Textarea it does **NOT** call `useFieldContext`, so inside a `Field` nothing auto-wires (label click won't focus, `FieldError` not tied via `aria-describedby` → SR gets state without message). Spawned an a11y fix task. Figma node 706:18439 (it's an instance of `text-input`, no separate variants); no Code Connect. Workflow-assisted (3-judge blind panel + code-aware pass → code-aware clean, 4 MED-driven fixes → all-HIGH) |
| NumericBadge | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/NumericBadge/NumericBadge.llm.md) | Counts chip; type = surface-driven (taste), 99+ cap, show-0 default, never clickable |
| OverflowList | Light | ☐ To do | — | — | |
| OverflowTooltip | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/OverflowTooltip/OverflowTooltip.llm.md) | The **conditional** `Tooltip` sibling — reveals full text **only when it's actually clipped** (`truncate`/`line-clamp`), silent when it fits. Code-only utility (no Figma/Code Connect, Artem: engineering component); compound built on `Tooltip`, perf-conscious (lazy ResizeObserver/MutationObserver on pointer-enter → cheap in virtualized tables/lists). **Boundary: conditional-on-overflow (this) vs always-on (`Tooltip`)**; extra/different content → `Tooltip`; interactive → `Popover`; preventing truncation → fix layout; essential clipped data → don't gate it behind hover-only (add copy/detail). **Rules that make it fire:** the clip class must sit on the *cloned* trigger child itself; content mirrors the trigger's full value. Knobs: `forceTooltip` (uniformity across mixed rows), `maxWidth` (def 400), `positioning` passthrough. Prime hosts: `Table` cells / rows / chips. **Skipped references** (thin technical wrapper; boundary already framed from Tooltip's side). Blind 6/6 HIGH → hardened forceTooltip use / wrong-node trap / essential-data a11y; code-aware clean. |
| Pagination | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Pagination/Pagination.llm.md) | Page-through nav for a **bounded** dataset (Ark UI compound; prev/next + numbered `PaginationList` w/ auto ellipsis + `PaginationPageSize` rows-per-page Select). Controlled OR uncontrolled. **Lead boundary (the #1 AI call): paginate when users *navigate* a known set (jump, return to page 2, pick page size) vs `Table` infinite-scroll + virtualization when they *graze* a stream** — Artem to confirm house stance. **Data wiring:** client = `useClientPagination(rows, 10)` → spread `{...pagination}` + render `pageData` (slices+clamps); server = controlled `count`+`page`/`onPageChange`+`pageSize`/`onPageSizeChange` (fetch on change); URL = `type='link'`+`getPageUrl`. **Locked:** range+ellipsis computed from `siblingCount`/`boundaryCount` (def 1); **no "1–10 of 200" total built in** (render your own Text); `size` medium32/small24; `align` left(def)/center/right; page-size is a Select (don't add another). **Table does NOT render it — compose `<Pagination>` below**; one `useClientPagination` feeds both. Boundaries: short set → none; steps/views → Tabs/stepper. Composition: full / numbers-only / prev-next-only. Figma node 11045-4121. Blind 10/10 HIGH, code-aware clean |
| Popover | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Popover/Popover.llm.md) | **Non-modal overlay anchored to a trigger, holding rich custom content** (Ark UI; Popover + Trigger + Content). Artem's framing: a **layout** component, deliberately general-purpose ("hard to name one exact case") → the **middle rung of the overlay ladder: richer than `Tooltip`, lighter / non-blocking vs `Dialog`/`Drawer`**; lead by role, not a use-case list. **Boundaries:** passive hover label → `Tooltip` (incl. rich-ish chart-stat readouts on *hover* — Popover opens on *click*); command list → `DropdownMenu`; blocking/destructive decision → `Dialog`; full detail / long panel → `Drawer`→page; form-field value → `Select`; never auto-open on load (feature promo → `Tour`/beacon; Pajamas: "do not show on page load"). **Locked:** non-modal + user-triggered (page stays live; dismiss outside-click/Esc), `PopoverTrigger` `asChild` (defaults NOT asChild), fixed look (surface-2 / rounded-12 / border / shadow / p-12 / portal / anim), **content is a raw slot you compose — NO Title/Description/Close/arrow sub-parts** (design-TBD per the Notes frame "decompose?/slots?"), built-in `ScrollArea` + min/max W·H clamp. Knobs: `min/maxWidth` · `min/maxHeight` (`PopoverSizeDimension` = `px`/`auto`/`unset`; def ~256–560 / 156–560 — keep bounded, wants >560 → climb the ladder), controlled `open`/`onOpenChange`, nesting sparingly. No Code Connect; Figma node 2619-11540. **Consulted GitLab Pajamas** (corroborated non-modal / rich-or-actionable-else-tooltip / no-page-load). Blind 8/8 (7 HIGH + 1 MED→fixed: hover readout = `Tooltip` vs click = Popover); code-aware clean. |
| Progress | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Progress/Progress.llm.md) | The **linear progress bar** (Ark UI) — determinate or **`value={null}` indeterminate**; **linear-only** (no radial). `size` xs–lg, full color palette (stay `brand` unless a status applies), `showLabel` for the `%`. **Use when progress is measurable, or the job is long / multi-step** (upload / export / batch / wizard) — incl. a long *unmeasurable* wait as `value={null}`; short / in-context → `Loader`, whole-layout load → `Skeleton`. A11y built in (Ark `progressbar` / `aria-valuenow`). Part of the **loading-feedback ladder** (judgment backlog). Figma 9881-937 (Notes stale — code ships more than that frame: sizes/colors/%/animation all already there). Blind 8/8 (family panel) after reconciling the long-unmeasurable-wait gap. |
| Radio | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Radio/Radio.llm.md) | Single-select one-of-a-set picker (`RadioGroup` owns the value; each `Radio` self-labels via `RadioLabel`). **Always a group** — a lone Radio is meaningless (→ Checkbox/Switch). **Boundaries:** multi → Checkbox; long list / save space → Select (~5-or-fewer judgment, mirrors Select); single on/off → Switch; 2–4 inline view toggle → SegmentedControl; independent toggles → ToggleButton row. **Self-labels → no FieldLabel**; group name = `FieldSet`+`FieldLegend` (the legend IS the name); validation on the group, not per option. **Locked:** vertical column only, `variant='default'|'card'` on the group (a design-taste call, NOT description-driven; card text-only today), `RadioIndicator` zero-prop/manual, a description switches the item to a 2-col grid, locked typography. **Emphasis = a `Badge` in `RadioLabel`** (designer-confirmed; it's a flex row built for it), never recolor. Default-selection left to context (AI decides). Figma node 290:5712; 2 Code Connect files. Workflow-assisted (8-agent grounding + 3-judge panel → all-HIGH, code-aware clean) |
| SegmentedControl | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/SegmentedControl/SegmentedControl.llm.md) | Compact **inline single-select** — 2–4 (up to ~7) mutually-exclusive segments, exactly one wins, switches a view/mode *now* or holds a compact form value. Ark `SegmentGroup` (radio semantics); emits a value, **owns no panels**. **Tiebreakers (the recurring ones):** form-column question w/ descriptions → `Radio`; page-level panel nav → `Tabs` (owns panels); same pill that *renders* the panel → `SegmentedTabs` ("does it manage the panel?"); independent multi-toggles → row of `ToggleButton`s; binary → `Switch`; long list → `Select` (past ~7). `SegmentedControlItem` = the only selectable child (unique `value`); `SegmentedControlButton`+`Separator` = the manual More-overflow (button is NOT selectable). **Not individually self-labelling** — wrap the *whole* control in a `Field` in forms (never per-segment); stands alone as chrome. **Locked:** no size/color/card variant (28px floor), `fullWidth` + per-item `disabled` the only knobs, auto sliding indicator, single-select (no deselect-to-empty), no outer margin (main removed the `mb-8` in v0.64.1). `NumericBadge`/icons compose in segments; icon-only needs a `Tooltip`/`aria-label`. Figma 634:11722 + Code Connect. Workflow-assisted (7-agent grounding + 3-judge panel → **30/30 HIGH**; code-aware clean, 1 nit fixed) |
| SegmentedTabs | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/SegmentedTabs/SegmentedTabs.llm.md) | The **segmented-pill tab set** — `SegmentedControl`'s look + `Tabs`' behavior (built on the same Ark UI tab primitive, **owns its panels** via `SegmentedTabsContent`, imports SegmentedControl's skin + sliding indicator). **Lead case: scoped views / quick-filters over ONE collection** — the same `Table`/list under preset mutually-exclusive scopes (All / Active / Pending / Blocked · All / Critical / saved "Custom view"); pills carry a `NumericBadge` count + an `indicator` dot for a saved view (Polaris "list-view with different filters applied"). **2nd case: a secondary / nested tab level under primary `Tabs`** (the pill skin distinguishes the levels; **2 levels max** — Material "secondary tabs establish hierarchy", Pajamas "max 2, last resort"). **Boundaries (the triangle):** an owns-no-panel view/mode toggle of the *same* content (table↔card, list↔board) → `SegmentedControl`; primary page sections → `Tabs` (underline = default, segmented = deliberate — Primer); large / combinable filters → `Select` / `FilterInput`. **Locked:** owns its panels (lazyMount/unmountOnExit on — don't hand-roll show/hide); auto sliding indicator; **overflow is a MANUAL "More" menu, NOT auto-scroll** (the Tabs difference); **no `size` prop** (fixed 28px; sizing is Figma-TBD "as in tabs"); no pressed state. **Unique:** the `indicator` dot, **actionable pills** (`SegmentedTabsTriggerButton` → per-pill `DropdownMenu`; `Tabs` uses bar-level `TabsLineActions`, `SegmentedControl` has none), `fullWidth`. Figma 1949-4862 + Code Connect (1956-8100). Consulted Polaris / Material / Carbon / Primer / Pajamas (boundary research). Blind 2-judge panel 8/8 → fixed the overflow-vs-switch-component tie-breaker + the scope-vs-presentation distinguisher. |
| Select | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Select/Select.llm.md) | Full **router** file: one doc covers the whole compound family (Artem maintains 3 Figma pages — button/input/menu — but code is one `Select/` dir, so the doc maps to code = one file, Figma pages reframed as renditions). `SelectInput` (canonical labelled form field, 36px, only trigger rendering multiselect as removable Tags + "+N" overflow + clear) / `SelectButton` (toolbar/inline/standalone; excludes primary+destructive at type level, locked large+fullWidth) / `SelectButtonTag` (emerging chip-trigger). Boundaries: ≤~5 → Radio/Checkbox/SegmentedControl (judgment, form-height driven); commands → DropdownMenu **with the one sanctioned "Create new…" action-row exception**; no Combobox → Select + `SelectSearchInput` (controlled; you own `collection.filter()`); free-entry → `Input`; result-set → FilterInput. Multiselect: default toggle commits immediately, Cancel/Apply footer+counter = optional staged-apply. Locked: fixed positioning, lazy-mount, `createListCollection` required, 280–320 width clamp, `SelectSeparator`-not-`DropdownMenuSeparator`. One size ships; smaller sizes + submenus/spinners/shortcut-chips fenced don't-build-yet. Action-oriented placeholder. Workflow-assisted (11-agent grounding + 3-judge blind panel, all-HIGH, code-aware clean). Figma 316:12047 / 114:2104 / 203:3915. Opens the forms/inputs family |
| Slider | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Slider/Slider.llm.md) | The **range-input control** — drag a handle to pick an approximate / bounded value or a **min–max range** (Ark UI slider). **Lean compound** (Switch/Checkbox idiom, not full Ark decomposition): root `Slider` owns the machine + parts `SliderControl` (track+range) / `SliderThumb` (the real `role=slider` node) / `SliderMarks` / `SliderInput` / `SliderValue`. **Boundaries:** exact figure (port/timeout/count) → `NumberInput`; huge range (1px = many units) → `NumberInput`; 2–7 fixed choices → `SegmentedControl`/`Radio`/`Select`; date/time → `DateInput`/`TimeInput`; on/off → `Switch`. **Ordinal tie-break (stress-test fix):** a magnitude you *dial* (Low→High sensitivity) = marked slider; named categories *even when ordered* (log level) = SegmentedControl — "feeling how much" vs "picking which one". **Locked:** single-vs-range = value LENGTH not a flag (`[50]` vs `[20,80]`, one `SliderThumb index` per entry, thumbs never cross); focus REUSES hover (enlarge+lift, no ring); disabled dims the WHOLE control 50% at root; **one size, horizontal only** (no sm/md/vertical, don't shrink via className); `error`→Ark `invalid` = danger handle only (message = `FieldError`); snap to a sane `step`; **tooltip XOR persistent value**. **Value readout = pick ONE:** beside-label / on-drag `tooltip` / inline `SliderInput` (numbers-only, commits on blur+Enter, the precision escape hatch). **Reads `Field` context** (unlike NumberInput — closes that gap; FieldDescription/Error still not `aria-describedby`-tied = DS-wide forms gap). Analytics land per-`SliderThumb` (`PRICE_MIN`/`PRICE_MAX`) — closed the CG-1 range-attribution gap. Built designer+AI from-zero-to-merge (PR #192 → v0.69.0); flat→lean-compound rework at maintainer request. Figma `slider` 11354:181 + `slider-input` 11358:1807; Code Connect = 5 examples (single/range/ticks/input/Field). Guide verified code-accurate + **3-judge blind stress-test 6/7 → all-HIGH** after the ordinal tie-break fix |
| Stack | Light | ☐ To do | — | — | Layout → defer to foundations |
| Flex | Light | ☐ To do | — | — | Layout → defer to foundations |
| Switch | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Switch/Switch.llm.md) | Single on/off **setting applied IMMEDIATELY** (state IS the value — dark mode, notifications); no submit. **Boundaries:** applied-on-Save / consent → Checkbox (don't bolt a Save step onto a Switch); press-to-flip toolbar/formatting → ToggleButton (closes that cross-ref); one-of-set → Radio; 2–4 inline view toggle → SegmentedControl. Self-labels via `SwitchLabel` → no FieldLabel; a SET → `FieldSet`+`FieldLegend` (**no `SwitchGroup`** — stack with VStack). **Locked:** `SwitchControl` zero-prop / placed first, NO size/variant/card, a description → 2-col grid, disabled on root, validation on the Field. `a11yMode` (opt-in) overlays a ✕/✓ glyph for color-independent on/off — recommend on for settings screens. Emphasis = `Badge` in `SwitchLabel`. Arrangement = control-left/label-right (ordinary forms); right-alignment is a layout taste call for advanced layouts (no prop). NO Code Connect file; Figma node 290:8253 (right-alignment / card / group-wrapper all Figma-ahead). Workflow-assisted (7-agent grounding + 3-judge panel → all-HIGH after 1 fix; code-aware clean, `a11yMode` confirmed real) |
| Tabs | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Tabs/Tabs.llm.md) | Switch between **a few peer content sections, one panel at a time, without leaving the page's context** (Ark UI compound: `Tabs` + `TabsList` + `TabsTrigger` + `TabsContent` + optional `TabsLineActions` / `TabsSeparator`). **Lead differentiator: Tabs OWNS its panels** — every `TabsTrigger value` pairs with a `TabsContent value`; `lazyMount` / `unmountOnExit` on by default; **don't hand-roll show/hide** (that's what separates it from `SegmentedControl`, which owns no panel). **The "which switcher?" triangle (the real risk):** primary, line-style content division of a page/section, owns the panels → `Tabs`; a compact **pill switch inside a component** (card/widget) that owns a *small* panel → `SegmentedTabs`; a compact pill that emits a value / flips a view of the *same* content (list↔board, table↔cards) and owns **no** panel → `SegmentedControl`; sibling sections **open at once** → `Accordion`. Off-ramps: distinct top-level areas → primary nav; hierarchy → `Breadcrumbs`; a sequence/wizard → stepper; a filtered list → `FilterInput`. **Locked:** one active tab + auto animated underline indicator (don't build your own); **overflow is automatic** — horizontal scroll + arrow buttons + a "more" affordance, **never wraps**; structure = `TabsList` (bottom border + indicator) holds triggers + right-aligned `TabsLineActions`. **Knobs:** `size` medium(def)/small; `variant` default(brand underline)/grayscale(quieter/nested); `value`/`defaultValue`/`onChange` (controlled when tab ↔ URL state); keep to **≈2–6** tabs (past ~7 → real nav or `Select`). Can sit in a `Drawer`/`Dialog` header (shipped). Figma 626-1442. Consulted GitLab Pajamas (one-at-a-time peer sections, keeps context). Blind 8/8 (7 HIGH + 1 MED → tightened the tab-count ceiling, the settings-vs-nav line, and the Tabs↔SegmentedTabs functional decider). |
| Tag | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Tag/Tag.llm.md) | The interactive chip; routes Tag vs SelectButtonTag vs FilterInput; Selectable = TBD |
| Textarea | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Textarea/Textarea.llm.md) | The multi-line free-text sibling of `Input` — a bare label-less `<textarea>` reusing Input's `inputVariants` box (+ `textareaVariants` `min-h` per size). **Boundary:** single line → Input, multi-row prose → Textarea; rich-text/formatting → not here (plain text only); **no `InputGroup` adornments** (those are Input's). **Locked:** resize = native `both`, **can't be constrained** (no prop, `className` `Omit`'d) — no auto-grow/rows/max-height, only `min-h`; decorative `pointer-events-none` grip; `size` = start-height only (not text size, no dense variant); `error` red + aria-invalid; needs a `Field` for label (auto-wired); validation double-wire. Figma node 106:126402; Code Connect = 3 examples (default/error/disabled). Char counter + auto-resize + 3 sizes are Figma-ahead/unresolved → fenced. **Contract gaps chipped (`task_b80cfa1c`):** missing `data-slot` (rule violation), `Omit`s className, prop-merge overrides field a11y (ref is FINE — attaches via the spread). Workflow-assisted (7-agent grounding + 3-judge panel → all-HIGH after fixing a self-contradictory resize bullet; code-aware clean) |
| TimeInput | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/TimeInput/TimeInput.llm.md) | The type-in-place **time-of-day** field (React Aria `useTimeField`; reuses DateInput's internal renderer, Clock icon). Value = `TimeValue` (`new Time(22,0)`), not a JS `Date`/string. `granularity` `minute` (default)/`hour`/`second`. **`showTimeDropdown`+`timeStep` always apply** (it's always a time — unlike DateInput where they're gated behind time granularity). 12h/24h app-wide via `DateFormatProvider` (no `hourCycle` prop). Same field-context gap + error double-wire. Boundaries: date or date+time → DateInput `granularity='minute'`; **duration/timeout/TTL → NumberInput** (a time-of-day ≠ an interval); few fixed times → Select; display → FormatDateTime. Blind 8/8 HIGH, code-aware clean |
| Toast | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Toast/Toast.llm.md) | Imperative `useToast`/`toaster` + one `Toaster`; boundary vs Dialog/Banner/Field; locked type-color/stacking/auto-dismiss; carries house microcopy → seeded the new [content-guidelines doc](./ai-ready-ds-content-guidelines.md) |
| ToggleButton | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/ToggleButton/ToggleButton.llm.md) | Actions-family **stateful** sibling of Button — on/off pressed state via `active`+`onToggle` (**not** onClick); smaller matrix `outline`/`ghost` × `brand`/`neutral`; designer default **ghost/neutral** (code defaults outline/brand — flagged as possible code change); a set of toggles = **row of ToggleButtons, not SegmentedControl**; Switch=setting vs ToggleButton=press-to-flip. Figma 295:5732 (Documentation prose; Notes = 2 design TODOs). Flagged **aria-pressed a11y gap** → spawned fix task |
| Tooltip | Light | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Tooltip/Tooltip.llm.md) | Hover/focus **passive label** for brief, **nonessential** context (Ark UI; compound Tooltip + Trigger + Content). **Lead use: name an icon-only control** (`TooltipTrigger asChild` wraps the real *focusable* element). **Boundaries:** interactive content / more than a brief hint → `Popover`; truncated text → `OverflowTooltip` (dedicated sibling — **we diverge from Pajamas**, which folds overflow into tooltip); essential / persistent / error → messaging ladder (`Field`/`Alert`/`Banner`/`Toast`); defining a word in prose → inline, not a tooltip on a non-focusable `<span>`; the obvious (close ✕) / repeating a visible label → skip. **a11y trap:** the tooltip is supplementary, **NOT** the accessible name — an icon-only control still needs its own `aria-label`. **Locked:** dark surface / xs-medium / rounded / 6px offset / auto-flip / portal / hover-focus-Esc — all automatic, don't restyle; **`Kbd` inside is first-class** (auto-restyled borderless/inverted). 3 shapes: default / title+description / label+Kbd. `interactive` only lets the pointer *reach* passive content, not host controls. **Figma-ahead flagged:** a max-width / line cap is drawn but not shipped. Code Connect present (node 269-10037). **First use of the new GitLab Pajamas reference** (+ Nord). Blind 7/8 HIGH → all-HIGH after closing the prose-term off-ramp + the nonessential-vs-only-cue tension; code-aware clean (caught the no-max-width Figma-ahead). |
| Tree | Light | ☐ To do | — | — | **New compound (v0.68.x), untracked until now.** Hierarchical / collapsible list: `Tree` + `TreeItem` / `TreeItemHeader` / `TreeItemContent` / `TreeItemIcon` on an internal `TreeBase`. Needs a guide — tier + assignee TBD (Artem to slot). |
| AnimatedBackground | Skip | ➖ N/A | — | — | Decorative |
| ButtonBase | Skip | ➖ N/A | — | — | Internal base for Button |
| Checkmark | Skip | ➖ N/A | — | — | Leaf primitive |
| DateFormatProvider | Skip | ➖ N/A | — | — | Provider |
| FormatDateTime | Skip | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/FormatDateTime/FormatDateTime.llm.md) | Tier overridden by Artem — date hand-formatting is a top AI-invention zone |
| Heading | Skip | ➖ N/A | — | — | Typography → foundations |
| Kbd | Skip | ➖ N/A | — | — | Leaf primitive |
| Loader | Skip | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Loader/Loader.llm.md) | **Tier overridden** (Skip→Done, like FormatDateTime — the which-loading-indicator choice is an AI-decision zone). The **spinner** — always indeterminate; `type` circle (+ `background` ring) / sonner, sizes 12–48, icon colors. **Short (~1–10s), in-context** waits (button / chart / module); NOT a whole-page first load (→ `Skeleton`), NOT measurable / long (→ `Progress`), NOT <1s. **a11y gap:** bare SVG — no label / `role=status`, always animates → the consumer adds the announcement + reduced-motion (parked as a foundations gap). Code Connect (node 856-394). Part of the loading-feedback ladder + the loading-a11y-gap row (judgment backlog). Blind 8/8 (family panel). |
| Logo | Skip | ➖ N/A | — | — | Leaf primitive |
| WallyIcon | Skip | ➖ N/A | — | — | Brand / mascot icon — decorative (new from main, v0.66) |
| Overlay | Skip | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Overlay/Overlay.llm.md) | **Tier-overridden (Skip→Done, Artem's call)** — dull, but the boundary is high-value. The house **backdrop / scrim** primitive: `fixed inset-0` dim + `backdrop-blur-xs` (`component-dialog-overlay` token) behind an elevated surface. **Presentation ONLY** — a styled `<div>`, no focus-trap/dismiss/scroll-lock/portal; modality comes from wrapping it in a headless `Backdrop` (`asChild`), exactly how Dialog/Drawer/Tour do it. **Reach for the owning component, not this:** modal → `Dialog`, side panel → `Drawer`, spotlight walkthrough → `Tour` (the cut-out hole is Ark's `Tour.Spotlight`, **NOT** Overlay — Overlay is only the dim fill); popover/tooltip/menu = non-modal, no scrim; loading veil → `Loader`/`Skeleton`. Direct use only for a bespoke full-screen surface the DS lacks (you own the behavior). **Locked:** look is fixed (`className` `Omit`ted — can't restyle/recolor/un-blur, no variant/opacity knob); fade is **parent-driven** via `data-state` (a bare Overlay won't animate); z-index computed for drawer layering. **Figma-ahead (node 1700-1584, tagged "Not done"):** blur under review, a darker variant + dim animation/timing all TBD → don't pre-build. Importable via the `./*` subpath (`@wallarm-org/design-system/Overlay`) though absent from the root barrel. No Code Connect. Blind stress-test **6/6 HIGH** (traps pre-empted: spotlight≠Overlay, dead `data-state`) |
| Page | Skip | ➖ N/A | — | — | Layout shell → foundations |
| Polymorphic | Skip | ➖ N/A | — | — | Render utility |
| ScrollArea | Skip | ➖ N/A | — | — | Utility primitive |
| Separator | Skip | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Separator/Separator.llm.md) | **Tier-overridden (Skip→Done, Artem's call)** — a primitive, but carries real routing + a11y judgment. A 1px **divider** (`<div>`, CVA; `border-primary`; `role='none'` when decorative). **Routing is the headline:** inside a family use its dedicated separator (`DropdownMenuSeparator` / `SelectSeparator` / `TabsSeparator` / `FieldSeparator` / `NavRailSeparator` / `BreadcrumbsSeparator` / `DateRangeSeparator`); raw `Separator` only for generic content. **Boundaries:** spacing/rhythm → `Stack`/`Flex` gap (don't use a rule as a spacer or stack them); between `Card`s → their own edge; not decoration. **Locked:** colour+weight fixed (`border-primary`/1px, no dashed/colour/thickness variant, don't restyle via `className`); **carries its own margin via the `spacing` prop** (token scale — don't wrap in a margin `div`); vertical `self-stretch`es → needs a bounded-height row. **a11y:** `decorative` defaults **true** (`role='none'`); `decorative={false}` (`role='separator'`) only for a real standalone group boundary — family separators own their context semantics (`FieldSeparator` forwards `decorative`). Figma 314-8100 ("Not done" tag; Notes empty) + Code Connect (orientation enum). Blind stress-test 6/7 → all-HIGH after scoping the decorative-vs-family-separator interaction |
| Skeleton | Skip | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Skeleton/Skeleton.llm.md) | **Tier overridden** (Skip→Done, with the loading family). The content-shaped **placeholder** — **wrap** real content (`<Skeleton loading>{…}</Skeleton>` reserves its exact box → no layout shift, flips to content when `loading=false`) or **standalone** `width`/`height`. `rounded` matches the element (def 6, `full` for avatar), `transparent` (def, animates over invisible content) vs filled `surface-1`; restrained pulse. **One primitive — compose** for text-lines / avatar / card (no prebuilt parts). **Use for a known-layout page/list/card/table load (~1–10s)**; NOT <1s, NOT single/inline (→ `Loader`), NOT dynamic-on-interaction (toast/dropdown/modal), don't pair with a spinner. Root `aria-hidden` (announcement on consumer — see a11y-gap row). Code Connect (node 5451-23064). Part of the loading-feedback ladder. Blind 8/8 (family panel). |
| SplashScreen | Skip | ➖ N/A | — | — | Full-screen state |
| TemporalCore | Skip | ➖ N/A | — | — | Date/time engine |
| Text | Skip | ✅ Done | Artem | [.llm.md](../packages/design-system/src/components/Text/Text.llm.md) | **Tier-overridden (Skip→Done, Artem's call)** — lives under Storybook `Typography/`; the type *scale* is foundation, but the **component** carries real routing judgment. The typography primitive for **all non-heading text** (polymorphic `<p>`, Radix `Slot` via `asChild`; type-scale + semantic colour tokens as CVA variants; `className` `Omit`ted). **Routing (the headline):** titles → `Heading`; a form-control label → `FieldLabel`; clickable → `Link`; mono/machine → `Code`; a domain value (status/method/IP/date/count) → the dedicated display component, never raw Text; and always Text over a raw `<p>`/`<span>`. **Locked:** no `className` (style via props); `size` = a type-scale STEP not a px (default body; a missing step = foundations conversation); `color` = semantic token & **de-emphasis is a ladder** (default `inherit`≈primary → `secondary` supporting → faintest tier for metadata; `danger` errors; `-alt` on dark surfaces); `weight` = emphasis not hierarchy. **Overflow:** `truncate` / `lineClamp={n}` + `OverflowTooltip` to recover. **Polymorphic** `asChild` for correct semantics. Figma node 2841-3722 was the Foundations *cover art* (no type-scale intent there); the scale belongs to the future foundations layer. No Code Connect. Consulted Nord (confirmed use-the-scale/tokens, don't hardcode). **2-judge blind panel 7/7 + 7/7 HIGH** → added the `FieldLabel` boundary + the colour de-emphasis ladder (both convergent nudges) |
| ThemeProvider | Skip | ➖ N/A | — | — | Provider |
| UtilityPage | Skip | ➖ N/A | — | — | Layout shell → foundations |

## Tiering notes
- **BulkBar isn't an importable component** — it's a shared surface + summary row composed by `SelectionBulkBar` (page/drawer) and `Table`'s action bar. Its `.llm.md` guidance therefore lives in **Selection** and **Table**, not a standalone file.
- **Table** wasn't in the original §8 deep list; it earned a (partial) sheet via the bulk-bar work and is worth a full pass later.
- **Light vs. Skip is provisional** — both depend on the §13.4 decision (write short notes for Light, or skip for now). Skip rows are providers, layout/typography primitives that defer to the foundations layer, and internal plumbing.

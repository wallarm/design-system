# `describe-component` — refinement log

> Parking lot for gotchas, gaps, and friction found while **running** the skill on real components. Don't stop a run to fix the skill — park it here in one line and keep authoring. Periodically (or when a batch is done), **harvest**: fold the confirmed ones into `SKILL.md` / the template, then mark them ✅.

## How to use
- **Capture (during/after a run):** the skill missed a step, made a wrong assumption, left something ambiguous, or you answered the same off-script question twice? Add a row. Keep going.
- **Harvest (periodic):** review 🅿️ rows, fold the real ones into [`SKILL.md`](SKILL.md), mark ✅ (or ❌ with a reason). The `skill-creator` skill (`/skill-creator`) is a good companion for the rewrite pass.

**Status:** 🅿️ Parked · ✅ Folded into skill · ❌ Won't fix

## Backlog

| Date | From run | Gotcha / gap | Suggested skill change | Status |
|---|---|---|---|---|
| 2026-06-12 | BulkBar | The tier-list target name was **not an importable component** — it was internal plumbing composed by other components. The skill jumps straight to "read `{Name}/` → save `{Name}/{Name}.llm.md`" with no check that `{Name}` is real. | Add an early **"verify the target"** step: confirm `{Name}.tsx` exists and `{Name}` is exported from the package index. If it's internal plumbing or a sub-component, redirect the file to the real exported host family — and tell the designer why. | ✅ Folded (2026-06-12) |
| 2026-06-12 | BulkBar | Author didn't know a **sub-component** (e.g. `SelectionBulkBar`, `TableActionBar`) can't be documented on its own — the metadata generator keys `.llm.md` strictly by **top-level component directory**. | In step 5 (Save), state the keying rule explicitly: a `.llm.md` only takes effect at `components/{TopLevel}/{TopLevel}.llm.md`; sub-component guidance belongs in the parent's file. | ✅ Folded (2026-06-12) |
| 2026-06-12 | BulkBar | The designer's verbal answer ("secondary + icon") **conflicted with Figma + code** (brand + icon). The skill has no guidance for resolving source conflicts. | In steps 3–4, add: when the designer's answer conflicts with Figma/code, surface the conflict, draft to the authoritative source (usually Figma), and flag that line for explicit sign-off. | ✅ Folded (2026-06-12) |
| 2026-06-12 | BulkBar | Figma settled the button recipe — but the designer volunteered the node URL only mid-interview (no Code Connect file, no node id in code). | Strengthen step 0: if no node id is in the code, ask for the Figma URL up front, and note that the designer often has it even when the code doesn't — it frequently settles styling and locked rules. | ✅ Folded (2026-06-12) |
| 2026-06-12 | BulkBar | A deep component (`Table`) needed only **one feature** (the action bar) documented now → wrote a partial sheet with a TODO. "Partial is fine" is in the skill, but not how to scope a partial when documenting a cross-cutting feature rather than the whole component. | Note in step 5: a feature-scoped partial is fine — lead with a one-line scope + a `TODO` banner naming what's still uncovered. | ✅ Folded (2026-06-12) |
| 2026-06-12 | BulkBar | (Strategy, not the skill) The §8 deep-tier list contained a non-component (BulkBar) and omitted one that needed a sheet (Table). | Validate the deep-tier list against actual package exports; tracked in `docs/ai-ready-ds-coverage.md`. | ❌ Won't fix in skill — strategy-level, resolved in the coverage tracker |
| 2026-06-12 | Badge | Storybook **argTypes controls lied**: the Playground dropdown omitted `small`, while the Sizes story + published Storybook render all three sizes — I flagged `small` as "semi-hidden" and the designer had to correct me (and the stale dropdown got fixed mid-run). | In step 1, read the **rendered stories**, not argTypes controls, as the source of truth for the sanctioned prop space; treat argTypes-vs-story mismatches as bugs to flag, not design signals. | ✅ Folded (2026-06-12) |
| 2026-06-12 | Badge | **Designer-default ≠ code-default trap**: designer says the standard type is `secondary`, but the code's defaultVariants is `solid`; first draft said "defaults to secondary" — an AI omitting the prop would silently get solid. Caught only by the sanity agent. | In steps 3–4: when the designer's stated default differs from the code default, write "set X explicitly — don't rely on the code default (Y)" and surface the mismatch to the designer as a possible code change. | ✅ Folded (2026-06-12) |
| 2026-06-12 | Badge | **Tier misjudged by fame**: template files Badge under "commodity → 3–4 lines or skip", but Badge is the *base of the domain-chip family* — its file is the router that keeps AI off raw Badge when HttpMethod/ResponseCode/Ip exist; it earned a full pass. | In the tiering table: tier by **role in the system** (base/host of a family ⇒ deep router file), not by how famous the component name is. | ✅ Folded (2026-06-12) |
| 2026-06-12 | Badge | Structured multi-choice form (AskUserQuestion) for interview round 2 was **dismissed** by the designer, who then answered the same questions happily in prose. | Note in step 3: interview in plain prose (voice-friendly, no anchoring options); reserve structured forms for picking the target component, not for judgment questions. | ✅ Folded (2026-06-12) |
| 2026-06-12 | Badge | The "blind" sanity agent (step 4) is great for doc-only steering, but a **code-aware re-check** accidentally caught two doc-vs-code contradictions (type default, onClick prop) the blind pass couldn't see. | Add a step 4b: after the blind check passes, one quick code-aware pass asking only "does any doc claim contradict the component source?" | ✅ Folded (2026-06-12) |
| 2026-06-12 | ResponseCode | The interview surfaced a **sanctioned rendition the component can't do via its props** (text-only `text-color` look) — designer intent ahead of the API. Resolved by documenting safe composition via the component's exported helpers instead of letting AI hardcode the mapping. | In step 3, add a stock question: "any sanctioned looks/uses this component can't express through its props yet?" — and when yes, document the escape hatch via exports, never via copied values. | ✅ Folded (2026-06-12) |
| 2026-06-12 | ResponseCode | Family siblings need **one voice**: the draft deliberately mirrored `HttpMethod.llm.md`'s structure/phrasing (they're twin chips), but the skill's step-1 table only says to read sibling *components*, not sibling *`.llm.md` files*. | In step 1, add: if a family sibling already has a `.llm.md`, read it and match its voice/section shape — divergent twins confuse the consuming AI. | ✅ Folded (2026-06-12) |
| 2026-06-12 | HttpMethod (revisit) | Docs written **before the family was mapped** aged: the original reference example lacked patterns its younger siblings proved (concrete click-parents, text-only rendition, casing trap found only on code re-read). Designer asked for the revisit himself. | Add a maintenance note: when a family base or sibling doc lands with new shared rules, sweep the family's earlier `.llm.md`s and back-port; re-run the sanity check on revisions like on fresh files. | ✅ Folded (2026-06-12) |
| 2026-06-12 | Country | Template's compound adjustment says "add required composition under **Locked**", but Country's composition is a menu of **sanctioned choices** (full / flag-only / name-only + strict order), not requirements — a dedicated `## Composition` section read better and the sanity agent cited it cleanly. | In the template: for compound components whose children are optional arrangements, allow a `## Composition` section (choices + when), keeping Locked for true non-negotiables. | ✅ Folded (2026-06-12) |
| 2026-06-12 | Country | Designer **declined a hypothesized hard rule** (flags-for-languages ban): "not a hard stop, case-by-case." The right move was deleting the bullet (doc silence = AI judgment), not softening it into mush — but the skill doesn't say what to do when a drafted rule is rejected. | In step 3: when the designer rejects a drafted rule, delete it rather than hedge it — a "sometimes" rule steers worse than silence. | ✅ Folded (2026-06-12) |
| 2026-06-12 | Ip | **Figma↔code divergences both ways**: Figma has Small/Medium size variants the code doesn't expose; code has a `horizontal` IpList mode Figma never documents. Doc followed CODE for what exists and Figma for intent — but the skill never states that precedence. | In step 1/0: state the precedence rule — code is ground truth for what EXISTS (never document a prop/variant that isn't shipped); Figma is ground truth for INTENT; divergences get flagged to the designer, not silently picked. | ✅ Folded (2026-06-12) |
| 2026-06-12 | Tag | **Designer had to ask** whether reference systems (step 2) were being consulted — the consult-or-skip decision was happening silently. (Tag was also the first component where step 2 genuinely applied; Nord contributed label-style rules + the moderation principle.) | In step 2: announce the decision out loud every run — "consulting X because common pattern" or "skipping references because domain primitive" — one line, so the designer never wonders. | ✅ Folded (2026-06-12) |
| 2026-06-12 | Tag | A Figma variant marked **TBD** (Selectable) exists in the component set while code ships an adjacent thing (`SelectButtonTag`). Omitting it from the doc would invite AI to invent a toggle tag; the fix was an explicit "TBD in design — don't build one yet" line. | Add to the template/step 3: design-TBD patterns get an explicit don't-build-yet line in the doc — silence invites invention; revisit when design settles. | ✅ Folded (2026-06-12) |
| 2026-06-12 | NumericBadge | **Step-2 reference sites defeat WebFetch**: Atlassian, Primer (and earlier Carbon) are JS-rendered — fetches return nav shells with no usage content. Only Nord worked. The consult step silently degrades to "announce + fail". | Note in step 2 which references are fetchable (Nord ✓; Carbon/Atlassian/Primer ✗ via plain fetch) and suggest a fallback: their GitHub docs sources or an llms.txt where available. | ✅ Folded (2026-06-15) |
| 2026-06-15 | Attribute | **Misclassified as a domain primitive**: step 2 lists `Attribute` among "Wallarm-domain primitives → skip references (no external system has them)", but a labeled key-value field is a common **description-list** pattern external systems ship (Ant `Descriptions`, Carbon structured-list, Atlassian). Skipping was still defensible (the distinctive bits — value hosts domain chips, `AttributeActions` investigate flow, read-only-now/edit-later — aren't in external docs), but the stated reason is wrong and could mislead the next author. | In step 2: move `Attribute` out of the domain-primitive skip list; classify by "does an external system ship this shape?" not by Wallarm-ness. A description-list IS a common pattern — consult is optional but not "skip, no external analog exists". | ✅ Folded (2026-06-15) |
| 2026-06-15 | Attribute | **Divergence ran the *inverse* direction** — Figma designed MORE than code ships (inline edit via Input/Select/DateInput + hover, plus an error state) where the usual case is code-ships-more-than-design-approves (NumericBadge). The folded precedence rule + design-TBD line both handled it cleanly, but the design-TBD guidance is phrased only for the code-ahead direction. | Minor: generalize the design-TBD note to "unshipped-either-direction gets a don't-build-yet line" — Figma-ahead-of-code is just as much an invention risk as code-ahead-of-design. | ✅ Folded (2026-06-15) |
| 2026-06-15 | Toast | **A THIRD parking lot was born:** the run surfaced rich UX-writing/microcopy guidance (sentence case, brevity, no "successfully", no trailing punctuation, loading-verb pattern, button-echoes-title). Artem directed: keep it in the component doc AND park the general form in a standalone **content-guidelines doc** (sibling to the design-judgment backlog) → future content/UX-writing skill. Created `docs/ai-ready-ds-content-guidelines.md` this run; SKILL.md step 6 still names only two parking lots. | In step 6 (and the "Self-improving" note): add the content-guidelines doc as a **third** parking lot — microcopy/UX-writing rules that generalize go there; component files keep local phrasing. Add a microcopy prompt to the step-4 sanity set when a component carries copy rules. | ✅ Folded (2026-06-15) |
| 2026-06-15 | Toast | **References can diverge from our shipped behavior in EITHER direction**, not just "we're stricter." EUI says "one toast at a time, don't stack" + "single action" — but our `Toaster` *stacks* (max 3 + hover-expand) and we allow up to ~3 actions; the right move was to hold our shipped/ designed behavior and cite the difference. The skill only tells you what to do when *we're stricter* than the reference. | In step 2: generalize "state our harder line" to "when our system differs from a reference *either way* (stricter OR more permissive), document our shipped behavior and cite the divergence — don't import the reference's rule over ours." | ✅ Folded (2026-06-15) |
| 2026-06-15 | Alert | **A `.figma.tsx` Code Connect file is the highest-value source for sanctioned compositions** — `Alert.figma.tsx`'s `example()` blocks spelled out every blessed arrangement (each color, closable, top vs bottom actions, code output) as real JSX, faster and more authoritative than reading stories. The skill's step-1 table mentions `*.figma.tsx` only as a place to find the node id. | In step 0/1: when a `.figma.tsx` Code Connect file exists, read its `example()` blocks as ground-truth composition recipes (variant → sanctioned JSX), not just for the node id. | ✅ Folded (2026-06-15) |
| 2026-06-15 | Dialog | **Step-0 "exported from the package index" check is too narrow** — Dialog is absent from the root barrel (`src/components/index.ts`) yet is a fully real, importable component via the `package.json` `"./*"` **subpath wildcard** (`@wallarm-org/design-system/Dialog` → `components/Dialog/index.ts`), which is exactly the `importPath` the metadata generator advertises. The generator also keys `.llm.md` purely by **directory scan**, so directory presence — not barrel membership — is what makes the doc take effect. A naive grep of the root index would have false-negatived a real component. | In step 0: verify exported-ness via (a) the component's own `index.ts` + (b) `package.json` `exports` (incl. `./*` subpath wildcards), not just the root barrel; and remember the generator keys by directory. Root-barrel absence is at most a minor consistency gap to flag, not a "not a real component" verdict. | ✅ Folded (2026-06-15) |
| 2026-06-15 | Dialog | **Designer's mental model can recategorize the component** — I'd slotted Dialog as "the blocking member of the messaging family"; Artem reframed it as a **layout** component (twin of Drawer / full-page), with confirmation/destructive/etc. as use-cases. Confirming the category up front reshaped the whole doc (layout ladder, not messaging). | In step 3 (or a pre-draft check): when a component could sit in more than one category, surface your assumed framing to the designer before drafting — a recategorization changes the lead, the boundaries, and the "reach for it when". | ✅ Folded (2026-06-15) |
| 2026-06-15 | Banner | **Content / microcopy guidance can be component-specific, not family-shared** — Toast & Alert share one "Content recommendation" frame, but Banner ships its OWN (institutional/factual voice, lead-with-fact, one-action-pattern) — a different register. Assuming the family shares a copy frame would have missed Banner's distinct rules + the "still relevant on another page?" boundary heuristic. | In step 1 (Figma read): check each component's own content/notes frame even within a family; don't assume a sibling's copy guidance transfers. Register can differ (terse toast vs institutional banner). | ✅ Folded (2026-06-15) |
| 2026-06-16 | Button | **Two-axis components (emphasis × intent, or any A×B style matrix) need per-scenario recipes, not just per-axis descriptions** — the blind check went MED on every "which variant within a color" call (the destructive variant, the dark-surface variant, the off-grid `brand`+`outline` ask) even though each axis was described clearly on its own; the HIGH-confidence fix was naming the concrete combo for each common case. | In step 1/4: for a component with two independent style axes, write worked recipes per common scenario (main / secondary / destructive / dark-surface) and make the step-4 tricky prompts target axis *combinations* (off-grid combos, "which B for this A"), not single-axis lookups. | 🅿️ Parked |
| 2026-06-16 | Button | **`get_metadata` on a full Figma *spec* page overflows the token limit** (213k chars — a documented component page packs Documentation + Notes + Anatomy + the full variant matrix). Recovered by jq-grepping the saved tool-result for the named frames' node IDs, then screenshotting just the `Documentation` / `Notes` frames at readable resolution. | In step 0/1: on a full spec page, don't expect `get_metadata` to fit — screenshot the page to see its frames, grep the saved metadata result for the doc/notes frame node IDs, then `get_screenshot` those frames directly to read the prose. | 🅿️ Parked |

# `storybook-docs` — refinement log

> Parking lot for friction found while **running** this skill on real components.
> Don't stop a run to fix the skill — add a one-line row and keep writing.
> Harvest periodically: fold the confirmed ones into `SKILL.md` or the reference
> files, then mark them.

## How to use

- **Capture (during or after a run):** the skill missed a step, made a wrong
  assumption, left something ambiguous, or you had to answer the same off-script
  question twice? Add a row and carry on.
- **Harvest (periodic):** review the parked rows, fold the real ones in, mark
  them. `/skill-creator` is a good companion for the rewrite pass.

Keep the three parking lots distinct, because mixing them loses things:

| Lot | For |
|---|---|
| This file | friction with the **skill** |
| `docs/storybook-docs-findings.md` | real problems in a **component** |
| `docs/ai-ready-ds-content-guidelines.md` | **microcopy** rules that generalise |

**Status:** 🅿️ Parked · ✅ Folded into skill · ❌ Won't fix

## Backlog

| Date | From run | Friction / gap | Suggested change | Status |
|---|---|---|---|---|
| 2026-08-24 | Checkbox | The first draft asserted a wrapped-label rule three reference systems agree on, without checking our own classes — `Checkbox.tsx` uses `items-center`, so the claim was wrong for us. Caught by reading the source, not by the references. | Step 1 now says to check the component's own tokens and classes before asserting behaviour, and step 3 says to document what we ship and park the divergence. | ✅ Folded (2026-08-24) |
| 2026-08-24 | Checkbox | Consulted only the two most relevant systems (Nord, Pajamas), following `describe-component`'s selective rule. Artem corrected it: consult **all seven, every time**. | The roster is mandatory, with a convergence filter to keep the budget. | ✅ Folded (2026-08-24) |
| 2026-08-24 | Button + Slider (eval runs, found independently) | Step 2's Figma recipe **dead-ends**: you cannot walk from a component node up to the page holding it. `get_metadata` with no `nodeId` returns only the file's cover page and there is no parent traversal, so the `Documentation` / `Notes` frames are unreachable without a node id. Both runs were rescued by `{Name}.figma.tsx`, which hard-codes the node URLs. | Step 2 now opens with "read `{Name}.figma.tsx`", says the upward walk is impossible, and tells the author to say so and move on rather than burning the run. `{Name}.figma.tsx` added to the step-1 source table. | ✅ Folded (2026-08-24) |
| 2026-08-24 | Slider + Stack (eval runs) | The roster marked **GitLab Pajamas** fetchable; it now 302s to a GitLab auth flow and the browser fallback hits bot verification. Also, some systems simply don't ship a component (Carbon has no layout pages, Primer no slider), which reads as a failed lookup rather than a signal. | Pajamas marked unreachable with the open-source repo as fallback; added that a system having nothing to say is itself a finding — when several don't document a shape, the guidance has to come from us. | ✅ Folded (2026-08-24) |
| 2026-08-24 | Slider (eval run) | Existing prose can be **factually wrong**, not just over budget — `WithInput` claimed live-updating and clamping, while `SliderInput` holds an uncommitted draft and commits on blur or Enter. The skill only warned about length. | Step 0 now names three failure modes for inherited prose (over budget / wrong altitude / factually wrong) and says to verify existing sentences against the source as carefully as your own. | ✅ Folded (2026-08-24) |
| 2026-08-24 | Stack (eval run) | Both arms overran the 120-word restraint bar for a six-story primitive (201 with the skill, 306 without), so the assertion may be mis-calibrated even though the skill clearly worked (one sentence vs three). | Watch across more primitives before changing the standard; the budget is per-sentence, not per-page, so a six-story page legitimately carries six sentences. | 🅿️ Parked |
| 2026-08-24 | Switch | A backticked phrase that **wrapped across two JSDoc lines** rendered as a fenced code block on the page, splitting the sentence into three pieces. Invisible in the diff, obvious on the page. | Added to the prose standard: never let a code span wrap. A one-line scan catches it — any JSDoc line with an odd number of backticks. | ✅ Folded (2026-08-24) |
| 2026-08-24 | Slider | **Nearly lost 13 stories.** Replacing existing JSDoc with a multiline regex (`/\*\*.*?\*/` under `re.S`) matched across story boundaries and deleted 223 lines. Caught only because a story/sentence count came back 1/1; `git checkout` restored it. | Never rewrite existing JSDoc with a multiline regex. Anchor on the `export const <Name>:` line, strip the block immediately above it line by line, and **always re-count stories against `git show HEAD:<file>` afterwards**. Watch for single-line `/** … */` comments, which a block-oriented stripper misses. | ✅ Folded (2026-08-24) |
| 2026-08-24 | Date family | Inserting the `DESCRIPTION` const by matching `const meta = {` **silently missed** three files that declare `const meta: Meta<typeof X> = {`. The pages then failed at runtime with "DESCRIPTION is not defined" — and **typecheck did not catch it**, so lint and tsc both passed on a broken page. | Anchor on `const meta` without assuming the form, and after writing, assert the const exists: `grep -c '^const DESCRIPTION' <file>` must be 1 wherever `component: DESCRIPTION` appears. Step 5's page check is what caught this — it is not optional. | ✅ Folded (2026-08-24) |
| 2026-08-24 | TimeInput + DateRangeInput | **Annotation labels missed entirely.** Forty-two `<Text size='sm' color='secondary'>` labels were left in the old style while `DateInput` next to them was converted, so the family shipped visibly inconsistent. The annotation check had been run for the Inputs folder and simply not repeated for the date family. | The annotation section now carries a mandatory `grep -n "<Text \|<Heading"` scan per page, with the label-versus-content test spelled out, and the scan is in step 5's checklist. | ✅ Folded (2026-08-24) |
| 2026-08-24 | Typography group | Converting the labels on `Text` / `Heading` / `Code` / `Pixel` looked wrong and Artem reverted it: on a page whose purpose is judging a typeface, a second typeface beside the specimen makes the specimen unjudgeable. | The annotation section now carves out typography pages explicitly — they keep their own quiet `Text` labels. Reverted in place. | ✅ Folded (2026-08-24) |
| 2026-08-24 | OverflowTooltip | The annotation scan searched for `<Text>` and `<Heading>` only, so it **found nothing** on a page carrying sixteen labels written as `<p className='text-sm text-text-secondary mb-8'>`. A scan that reports zero is worse than no scan, because it reads as a clean result. | Scan widened to `text-text-(secondary\|tertiary)` as well as the components, with a note that a label is not always a `Text` and the conversion keeps the spacing classes. Re-ran it across every finished page: nothing else was hiding. | ✅ Folded (2026-08-24) |
| 2026-08-24 | Tour | Its in-canvas paragraphs were parked as a finding rather than fixed, on the grounds that removing rendered content was out of scope. Artem overruled: annotation text is the docs layer, so shorten it. | The annotation section now says a long annotation is a description in the wrong place, and to cut to the fewest words that still name what you are looking at. Five paragraphs removed, legend cut to one word per row. | ✅ Folded (2026-08-24) |
| 2026-08-24 | OverflowTooltip | Converting the labels to the house style left them all *too long* and one of them pointless: every label repeated the story name back ("Automatic detection - short text"), and `DynamicContent` carried a single label on a single specimen, which is pure duplication of its own heading. Artem asked for a stress test and it surfaced immediately. | Added the earning test — a label must tell two specimens apart, or it goes — and the instruction to cut a surviving label to the part that differs. Fifteen shortened, one removed, its meaning moved into the story sentence. | ✅ Folded (2026-08-24) |
| 2026-08-24 | Badge + NumericBadge | The widened scan **still** missed them: Badge's column headers are `<th className='p-8 text-left'>` and NumericBadge's row labels are bare `<div>Small</div>` with no classes at all. Third distinct shape in three folders, each invisible to the scan written for the previous one. | Scan widened again to include bare capitalised elements, but the section now says plainly that no grep finds all of them — the grep is a first pass and reading the rendered frame is the actual check. Expect a fourth shape. | ✅ Folded (2026-08-24) |
| 2026-08-24 | NumericBadge | The conversion **broke the layout**. Its size and type stories put labels in one `VStack` and specimens in parallel ones with `justify='between'`, which only lines up while both columns have equal row heights; the taller annotation style pulled them apart and the page looked crooked. A later fix then made a label wrap in its fixed-width column. | Added a layout check after converting: pair each label with its own row so alignment is structural, and widen a fixed-width label column rather than shortening the label. Both stories rewritten as one row per specimen. | ✅ Folded (2026-08-24) |
